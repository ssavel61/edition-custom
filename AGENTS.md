# Agent discovery adapter

This public repository is model-neutral. `CLAUDE.md` retains its filename for compatibility, but its project guidance applies to every AI collaborator.

On Santosh's machine, if `../ngw-dev/WORKING_AGREEMENT.md` exists, read it and `../ngw-dev/context/ABOUT_SANTOSH.md` before consequential work. Never copy their private content into this public repository.

Before doing work:

1. Read `CLAUDE.md` completely.
2. Confirm whether the task touches the auto-deployed theme or the separately deployed Worker.
3. Preserve the public-repository privacy and secret boundaries.
4. Use the documented validation and deployment gates.

Do not put durable project facts in this adapter. Update `CLAUDE.md` or the closest authoritative source.
