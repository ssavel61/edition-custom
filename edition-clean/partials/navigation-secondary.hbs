<ul class="nav">
    {{#foreach navigation}}
        <li class="nav-{{slug}}{{#if current}} nav-current{{/if}}">
            <a href="{{url}}" {{#if (or (match url "x\\.com") (match url "tiktok\\.com"))}}target="_blank" rel="noopener noreferrer"{{/if}}>{{label}}</a>
        </li>
    {{/foreach}}
</ul>
