{{!< default}}

<main class="site-main">

    <header class="feed-header gh-canvas">
        {{#if @custom.feed_title}}
            <div class="feed-header-wrapper">
                <h2 class="feed-header-title">{{@custom.feed_title}}</h2>
            </div>
        {{/if}}
    </header>

    {{!-- Custom text-only layout: No image placeholders, just clean content --}}
    <div class="post-feed gh-feed gh-canvas">
        {{#get "posts" limit="3" include="authors,tags" order="published_at desc"}}
            {{#foreach posts}}
                <article class="feed {{post_class}}">
                    <div class="feed-wrapper" style="grid-template-columns: 1fr;">
                        <h2 class="feed-title" style="margin-bottom: 8px;">{{title}}</h2>
                        
                        {{#if excerpt}}
                            <div class="feed-excerpt" style="margin-bottom: 12px; color: var(--secondary-text-color); font-size: 1.5rem; line-height: 1.5;">
                                {{excerpt words="25"}}...
                            </div>
                        {{/if}}

                        <time class="feed-date" datetime="{{date format="YYYY-MM-DD"}}">
                            {{date format="MMMM D, YYYY"}}
                        </time>
                    </div>
                    <a class="u-permalink" href="{{url}}" aria-label="{{title}}"></a>
                </article>
            {{/foreach}}
        {{/get}}
    </div>

</main>

{{#contentFor "body_class"}}{{#if @site.cover_image}} with-full-cover is-head-transparent{{/if}}{{#if @member}} logged-in{{/if}}{{/contentFor}}
